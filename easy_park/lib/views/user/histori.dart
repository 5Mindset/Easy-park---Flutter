import 'package:flutter/material.dart';

class Histori extends StatelessWidget {
  const Histori({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Histori Page'),
    );
  }
}