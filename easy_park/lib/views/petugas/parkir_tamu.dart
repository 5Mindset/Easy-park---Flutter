import 'package:flutter/material.dart';

class ParkirTamu extends StatelessWidget {
  const ParkirTamu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Parkir Tamu Page'),
    );
  }
}