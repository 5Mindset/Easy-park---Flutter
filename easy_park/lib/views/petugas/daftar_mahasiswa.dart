import 'package:flutter/material.dart';

class DaftarMahasiswa extends StatelessWidget {
  const DaftarMahasiswa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Daftar Mahasiswa Page'),
    );
  }
}