import 'package:flutter/material.dart';

class DaftarTamu extends StatelessWidget {
  const DaftarTamu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Daftar Tamu Page'),
    );
  }
}