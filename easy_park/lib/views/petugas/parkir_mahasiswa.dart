import 'package:flutter/material.dart';

class ParkirMahasiswa extends StatelessWidget {
  const ParkirMahasiswa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Parkir Mahasiswa Page'),
    );
  }
}